# Real Estate Management System - Python Dependencies
# Author: Luay Alkawaz
# Version: 1.0.0

# Core Kivy framework for GUI
kivy>=2.3.0

# Image processing for photo management
Pillow>=10.0.0

# Additional Kivy dependencies (automatically installed with kivy)
# kivy_deps.angle>=0.4.0     # Windows only
# kivy_deps.glew>=0.3.1      # Windows only
# kivy_deps.sdl2>=0.8.0      # Windows only

# Standard library modules used (no installation required):
# - sqlite3 (database)
# - configparser (configuration)
# - logging (logging)
# - pathlib (file paths)
# - datetime (date/time handling)
# - os, sys (system operations)
# - typing (type hints)
filetype>=1.2.0

# For future enhancements
requests>=2.32.0
